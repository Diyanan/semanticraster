<UserControl x:Class="Semantic.Controls.SegmentSuperPixelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Semantic.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel Orientation="Vertical">
            <TextBlock x:Name="spcountBlock" TextWrapping="Wrap" Text="预期分割数目"/>
            <TextBox x:Name="spcount" Height="23" TextWrapping="Wrap" Text="{Binding spcount}"/>
            <TextBlock x:Name="compactnessBlock" TextWrapping="Wrap" Text="紧致度"/>
            <TextBox x:Name="compactness" Height="23" TextWrapping="Wrap" Text="{Binding compactness}"/>

        </StackPanel>
    </Grid>
</UserControl>
