<UserControl x:Class="Semantic.Controls.SegmentJSEGView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Semantic.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             >
    <Grid>

        <StackPanel Orientation="Vertical">
            <TextBlock x:Name="tQuanBlock" TextWrapping="Wrap" Text="量化阈值"/>
            <TextBox x:Name="tQuan" Height="23" TextWrapping="Wrap" Text="{Binding tQuan}"/>
            <TextBlock x:Name="ThresholdBlock" TextWrapping="Wrap" Text="颜色阈值"/>
            <TextBox x:Name="Threshold" Height="23" TextWrapping="Wrap" Text="{Binding Threshold}"/>
            <TextBlock x:Name="MinsizeBlock" TextWrapping="Wrap" Text="最小像素数"/>
            <TextBox x:Name="Minsize" Height="23" TextWrapping="Wrap" Text="{Binding Minsize}"/>

        </StackPanel>
    </Grid>
</UserControl>
